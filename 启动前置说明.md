# 启动前置说明（空目录拉取项目）

本文说明：在一个空目录中 `git clone` 拉取本项目后，**需要手动准备哪些东西、改动哪些文件**，才能把前后端跑起来。

> 判断依据：根目录 [.gitignore](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/.gitignore)。凡是 `.gitignore` 忽略的内容，拉取后**都不会存在于仓库里**，必须手动补齐。

---

## 一、快速结论：拉取后缺什么

| 被 `.gitignore` 忽略的项 | 含义 | 是否必须 | 需要做什么 |
|---|---|---|---|
| `.env` | 环境配置（含数据库密码、LLM Key） | ✅ 必须 | 复制 `.env.example` 并填写 |
| `.venv/`、`venv/` | Python 虚拟环境 | ✅ 必须 | 新建虚拟环境并 `pip install` |
| `models/` | 本地向量模型（基座 + 微调产物） | ✅ 必须（向量） | 下载 `bge-large-zh-v1.5` 到 `models/system/` |
| `rerankers/` | 重排序交叉编码器 | ⚠️ 可选 | 下载 `bge-reranker-base`；不配则自动回退词法重排 |
| `data/*`（保留 `data/sample/`） | Chroma 向量库 / 数据集 / 上传文件 | ➖ 运行时生成 | 无需手动建，应用启动 & 使用时自动创建 |
| `logs/` | 日志目录 | ➖ 运行时生成 | 应用自动创建 |
| `uploads/` | 上传目录 | ➖ 运行时生成 | 应用自动创建 |
| `frontend/node_modules/` | 前端依赖 | ✅ 必须 | `npm install` |
| `temp/`、`/docs` | 论文/文档 | ❌ 无关 | 不涉及启动 |

> 结论：**必须手动补 4 类东西** —— ① `.env`、② Python 依赖环境、③ 本地模型 `bge-large-zh-v1.5`、④ 前端 `node_modules`；另需在 MySQL 里建好数据库。

---

## 二、需准备的基础环境

| 依赖 | 版本要求 | 说明 |
|---|---|---|
| Git | 任意 | 拉取代码 |
| Python | **3.12+**（[pyproject.toml](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/pyproject.toml) 要求 `>=3.12`） | README 写 3.11+，以 pyproject 为准 |
| Node.js | **20 LTS+** | 前端 Vite 8 建议较新版本 |
| npm | 随 Node 自带 | |
| MySQL | **5.7.6+**（推荐 8.x） | 全文检索依赖 InnoDB ngram 解析器 |

---

## 三、需要手动创建 / 下载的内容

### 1. 创建 `.env`

```bash
cp .env.example .env
```

然后编辑 `.env`，至少填写：

- 数据库：`MYSQL_HOST / PORT / USER / PASSWORD / DB`（密码必需）
- 大模型：`LLM_API_KEY`（DeepSeek 等 OpenAI 兼容服务）
- Embedding：可选远程 `EMBEDDING_API_BASE / EMBEDDING_API_KEY`，或保持本地模型
- 计算设备：`EMBEDDING_DEVICE`（`cpu` / `cuda` / `auto`，默认 `auto`，无显卡自动回退 CPU）

> 参考 [config.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/app/core/config.py) 中的默认值了解每个字段含义。

### 2. 创建 Python 虚拟环境并安装依赖

```bash
python -m venv .venv
# Windows PowerShell：
.venv\Scripts\Activate.ps1
# Linux/macOS：
source .venv/bin/activate

pip install -r requirements.txt
```

**本地向量模型依赖 torch，建议装 CPU 版以减小体积**（无 GPU 时必选）：

```bash
pip install torch --index-url https://download.pytorch.org/whl/cpu
```

> 有 NVIDIA 显卡需要 GPU 加速时，按官方指引装对应 CUDA 版 torch，并把 `.env` 的 `EMBEDDING_DEVICE` 设为 `auto` 或 `cuda`。

### 3. 下载本地向量模型（必需）

`.gitignore` 忽略了整个 `models/`，故基座模型需手动下载到 `models/system/bge-large-zh-v1.5`：

```bash
huggingface-cli download BAAI/bge-large-zh-v1.5 --local-dir models/system/bge-large-zh-v1.5
```

> 国内网络慢时先设置 HF 镜像：`$env:HF_ENDPOINT="https://hf-mirror.com"`（PowerShell）或 `export HF_ENDPOINT=https://hf-mirror.com`（bash）。下载后约 1.3GB。
>
> 不下载也可启动，但向量化会降级为「哈希特征向量」（[embedding.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/app/ai/embedding.py) 的 `_resolve_local_model_path` 找不到目录则回退），检索质量无法用于正式实验。

### 4. 下载重排序模型（可选）

`.gitignore` 忽略了 `rerankers/`。如需真实交叉编码器精排：

```bash
huggingface-cli download BAAI/bge-reranker-base --local-dir rerankers/bge-reranker-base
```

> 不下载也不影响启动：默认配置 `rerank_model=rerankers/bge-reranker-base`，加载失败会回退词法代理重排（[reranker.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/app/rag/reranker.py) 的 `build_reranker` 有 try/except 兜底）。

### 5. 创建 MySQL 数据库

依赖安装完后，配置里指定的数据库（默认 `ragchat`）需要提前建好，表结构由 Alembic 迁移自动创建：

```sql
CREATE DATABASE ragchat CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

> 数据库名要与 `.env` 中 `MYSQL_DB` 一致。

### 6. 安装前端依赖

```bash
cd frontend
npm install
```

---

## 四、需要检查 / 改动的文件

### 1. `.env`（必改）

按第三节第 1 步填写数据库密码、LLM Key 等。

### 2. `frontend/vite.config.ts`（需核对代理端口）

当前 [vite.config.ts](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/frontend/vite.config.ts) 中 `/api` 代理目标是 `http://127.0.0.1:8000`，而后端实际端口是 **4040**（见 [main.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/main.py) 的 `uvicorn.run(..., port=4040)`）。二者不一致会导致前端登录等接口报 `ECONNREFUSED`。

需将代理目标改为 4040：

```ts
proxy: {
  '/api': {
    target: 'http://127.0.0.1:4040',
    changeOrigin: true,
  },
},
```

> 说明：后端路由已带 `/api/v1` 前缀（[main.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/main.py) `include_router(api_router, prefix="/api/v1")`），所以代理**不要加 rewrite**，原样转发即可。

### 3. `main.py` / 端口（如需换端口）

若后端不用 4040，需同步改两处：[main.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/main.py) 的 `uvicorn.run` 端口、以及上面的 vite 代理目标。

---

## 五、分步启动流程

### 后端（端口 4040）

```bash
# 激活虚拟环境后
python main.py
# 或
uvicorn main:app --host 0.0.0.0 --port 4040 --reload
```

启动时 `lifespan` 会自动执行 `alembic upgrade head` 建表（[main.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/main.py) 的 `init_db()`）。若需手动迁移：

```bash
alembic upgrade head
```

验证：访问 `http://localhost:4040/health` 返回 `{"status":"ok"}`。

### 前端（端口 4041）

```bash
cd frontend
npm run dev
```

访问 `http://localhost:4041`，登录接口经 `/api` 代理转发到后端 4040。

---

## 六、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 登录报 `ECONNREFUSED` | vite 代理端口指向 8000，后端在 4040 | 改 `vite.config.ts` 代理 target 为 4040 |
| 启动报 MySQL 连接失败 | 数据库未建 / `.env` 密码不对 | 建库并核对 `.env` |
| 迁移报 FULLTEXT/ngram 错误 | MySQL 版本过低 | 升级到 5.7.6+ 或 8.x |
| 向量检索质量差 | 本地模型未下载，已降级哈希向量 | 下载 `bge-large-zh-v1.5` 并重启 |
| 后端报 `_SimpleFormatter` pickle 错误 | uvicorn reload 多进程问题 | 本项目已修复（类移到 `main` 块外） |
| 首次启动慢 | 本地向量模型加载较久 | 加载后走进程内缓存，属正常现象 |