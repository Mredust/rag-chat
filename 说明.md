# 模型微调硬件配置说明

## 一、结论速览

本项目微调的是 **Embedding（向量）模型**，不是大语言模型（LLM），硬件门槛远低于大模型微调。

| 场景 | 数据量 | 笔记本配置 | 云服务器配置 |
|---|---|---|---|
| 功能测试 | 1000 条 | 可纯 CPU 跑；建议带独立显卡 | T4 16GB / 8 vCPU 32GB 内存 |
| 正式实验 | 5 万条 | 需独立显卡 12GB+ 显存 | A10/RTX 4090 24GB 单卡，16 vCPU 64GB 内存 |

> 核心结论：**一台带 NVIDIA 独立显卡（8GB 以上显存）的笔记本即可完成 1000 条测试；5 万条实验推荐 16GB 以上显存（云上租 T4/A10 最划算）。**

---

## 二、本项目训练特点（决定硬件需求的关键）

先确认训练的实际形态，配置才能算准：

1. **微调对象**：Embedding 向量模型，默认基础模型为 `bge-large-zh-v1.5`（约 **3.35 亿参数 / 0.33B**，24 层、1024 隐藏维度）；也可导入 `Octen-Embedding-0.6B`（约 6 亿参数）。
2. **训练方式**：`sentence-transformers` 对比学习（`MultipleNegativesRankingLoss` + 难负样本），**全参数 AdamW 微调**（非 LoRA、非冻结骨干）。
3. **默认超参**（[ml_service.py](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/app/services/ml_service.py)）：`num_epochs=3`、`batch_size=16`、`lr=2e-5`、`passage_max_len=512`。
4. **当前代码限制**：训练与推理加载模型处写死了 `device="cpu"`（见 [ml_service.py:1532](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/app/services/ml_service.py#L1532) 与 [embedding.py:69](file:///d:/广州新华/毕业论文/3.%20毕设项目/rag-chat/app/ai/embedding.py#L69)）。**要使用 GPU 加速，需把这两处的 `device="cpu"` 改为自动检测（如 `device="cuda" if torch.cuda.is_available() else "cpu"`），否则即使租用 GPU 服务器也只跑 CPU。**

---

## 三、显存/内存估算原理

以 `bge-large-zh-v1.5`（0.33B 参数）全参数 AdamW 微调为例：

| 项目 | FP32（当前代码） | FP16 / 混合精度 |
|---|---|---|
| 模型权重 | 1.34 GB | 0.67 GB |
| 梯度 | 1.34 GB | 0.67 GB |
| AdamW 优化器状态（m+v） | 2.68 GB | 2.68 GB（保持 FP32） |
| 激活值（batch=16、seq=512） | 2~4 GB | 1~2 GB |
| **合计** | **约 8~10 GB** | **约 5~7 GB** |

> 预估口诀：`0.33B 全参微调 ≈ 8~10GB 显存（FP32）`；换用 `Octen-Embedding-0.6B` 时显存需求约翻倍（16~20GB）。

---

## 四、笔记本配置建议

### 场景 A：1000 条（功能测试）

- **最低配置（能跑，CPU 慢但可行）**：任意现代笔记本，**16GB 内存**即可，纯 CPU 跑（约 30 分钟~1 小时）。
- **推荐配置（体验好）**：
  - 显卡：NVIDIA 独立显卡 **6GB 以上显存**（如 RTX 3050/3060 Laptop 6GB）
  - 内存：**32GB**
  - 说明：6~8GB 显存足以放下 0.33B 模型的 FP32 训练。

### 场景 B：5 万条（开始实验微调）

- **显卡**：**12GB 以上显存**，推荐 RTX 4080 Laptop（12GB）/ RTX 4090 Laptop（16GB）。
- **内存**：**32GB 及以上**（数据加载与预处理占用）。
- **说明**：5 万条 × 3 轮 ≈ 约 9400 步，8GB 卡约每小时 1000~2000 步，总时长 **数小时到十几小时**；16GB 卡可更快。笔记本长时间满负载需注意散热与供电。

---

## 五、云服务器配置建议

### 场景 A：1000 条（功能测试）

- **显存**：T4 16GB（性价比最高）或 A10 24GB。
- **CPU/内存**：**8 vCPU + 32GB 内存**（训练代码会加载全部样例到内存并切分）。
- **磁盘**：100GB SSD（含模型、数据集、产出模型拷贝）。

### 场景 B：5 万条（正式实验微调）

- **显卡**：**单卡 A10 24GB / RTX 4090 24GB / L20 48GB** 任选其一（24GB 已非常充裕）。
- **CPU/内存**：**16 vCPU + 64GB 内存**（5 万条三元组的预处理与验证集计算）。
- **磁盘**：200GB SSD。
- **性价比方案**：若预算有限，**T4 16GB 单卡**也能跑 0.33B 模型（`batch_size` 可降到 8），时长约十几小时；按量付费即可。
- **注意**：若基础模型换成 0.6B 级（如 Octen-Embedding-0.6B），显存建议上到 **16~24GB**。

---

## 六、重要提醒

1. **先改代码再用 GPU**：训练/加载处 `device="cpu"` 已硬编码，租用 GPU 服务器前务必先改为 CUDA 自动检测，否则白租。
2. **本项目不是 LLM 微调**：0.33B 的向量模型门槛很低，**不需要 A100/H100** 这类大卡；被"微调"二字误导去租 80GB 显存是浪费。
3. **数据量对显存的边际影响小**：显存主要由模型大小 + batch_size + seq_len 决定，5 万条相对 1000 条主要增加的是**训练时长**而非显存。
4. **混合精度建议**：当前代码未开启 AMP（无 `autocast`/`GradScaler`），启用 FP16 混合精度可把显存从约 8~10GB 降到 5~7GB，让 6GB 显卡更稳妥，可考虑后续补充。